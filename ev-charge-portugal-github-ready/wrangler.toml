name = "broken-mud-373e"
main = "worker/index.js"
compatibility_date = "2026-09-25"

[assets]
directory = "./dist"
binding = "ASSETS"

# Cloudflare resource IDs are injected by the deployment workflow.
# Do not commit placeholder IDs here: Wrangler treats them as real resources
# and rejects the deployment before the Worker can be published.
