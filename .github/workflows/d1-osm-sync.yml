name: D1 OpenStreetMap station sync

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/d1-osm-sync.yml"
      - "ev-charge-portugal-github-ready/scripts/sync-d1-openstreetmap.mjs"
      - "ev-charge-portugal-github-ready/cloudflare/d1/schema.sql"
  schedule:
    - cron: "30 */6 * * *"

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      D1_DATABASE_NAME: ${{ secrets.D1_DATABASE_NAME }}
    defaults:
      run:
        working-directory: ev-charge-portugal-github-ready
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Validate Cloudflare configuration
        run: |
          test -n "$CLOUDFLARE_API_TOKEN" || { echo "::error::Missing CLOUDFLARE_API_TOKEN repository secret"; exit 1; }
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo "::error::Missing CLOUDFLARE_ACCOUNT_ID repository secret"; exit 1; }
          test -n "$D1_DATABASE_NAME" || { echo "::error::Missing D1_DATABASE_NAME repository secret"; exit 1; }
      - name: Install Wrangler
        run: npm install --global wrangler@4
      - name: Check Cloudflare D1 access
        run: |
          wrangler d1 list
          wrangler d1 info "$D1_DATABASE_NAME"
      - name: Ensure D1 schema
        run: wrangler d1 execute "$D1_DATABASE_NAME" --remote --file=cloudflare/d1/schema.sql
      - name: Download and prepare OpenStreetMap snapshot
        run: node scripts/sync-d1-openstreetmap.mjs
      - name: Upload snapshot to D1
        run: |
          set -e
          for file in tmp/d1-osm-sync/*.sql; do
            echo "Uploading $file"
            success=0
            for attempt in 1 2 3; do
              if wrangler d1 execute "$D1_DATABASE_NAME" --remote --file="$file"; then
                success=1
                break
              fi
              echo "Upload failed (attempt $attempt); retrying..."
              sleep $((attempt * 5))
            done
            test "$success" = "1" || exit 1
          done

# Manual connectivity test
